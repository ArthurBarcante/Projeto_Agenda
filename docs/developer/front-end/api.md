# API Front-End

A pasta `front/api` centraliza chamadas HTTP:

- `api.js`: autenticação, fetch autenticado e tratamento de erro.
- `tasks.js`, `events.js`, `progress.js`, `users.js`: módulos por domínio.
