# Camada Mock

A camada mock fica em `back/mock/db.json` e é usada somente para simulação local do frontend.

- Não substitui o backend real.
- Não possui regras de autenticação JWT.
- Serve para navegação rápida em desenvolvimento.
